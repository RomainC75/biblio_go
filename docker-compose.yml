version: '3.8'
services:
  db:
    image: postgres:12
    restart: always
    environment:
      - POSTGRES_USER=name
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=biblio
    ports:
      - '5432:5432'
    # volumes: 
      # - db:/var/lib/postgresql/data
      # - db:/var/lib/mysql
    # healthcheck:
    #   test: ["CMD-SHELL", "pg_isready -U postgres"]
    #   interval: 1s
    #   timeout: 3s
    #   retries: 30

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080
volumes:
  db:
    driver: local